"""Shared constants for Lumo core."""

# The curriculum version stored in DB records (UserState, ExerciseInstance).
# Must match the curriculum directory name under curriculum/.
CURRICULUM_VERSION = "v1"
